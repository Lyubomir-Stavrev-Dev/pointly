{
  "name": "pointly-website",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing + support site for Pointly (trypointly.com), deployed to Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev website",
    "deploy": "wrangler pages deploy",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4.16.0"
  }
}
