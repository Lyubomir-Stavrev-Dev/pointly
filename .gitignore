# Swift / Xcode build artifacts
.build/
DerivedData/

# Generated by XcodeGen from project.yml (run `xcodegen generate`)
Pointly.xcodeproj/

# App Store archive + exported package (build_appstore.sh output)
build/
*.xcarchive
*.dSYM
*.ipa

# App bundle (generated)
*.app/

# Build logs
build_output.log

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Cursor IDE workspace
.cursor/

# Xcode user data
*.xcuserstate
xcuserdata/

# SwiftPM resolved file (optional — uncomment to track)
# Package.resolved

# Release artifacts
Pointly-release.zip

# Claude Code local config
.claude/

# Node / Cloudflare Pages
node_modules/
.wrangler/
.dev.vars

# Marketing video pipeline — regenerable artifacts (keep raw takes + final cuts tracked)
marketing/video/.work/
marketing/video/cards/out/
marketing/video/auto/mousetool
marketing/video/auto/*.log
marketing/video/auto/.take-*.mov

# Website downloads — regenerated by release.sh (10MB+ binary)
website/downloads/
