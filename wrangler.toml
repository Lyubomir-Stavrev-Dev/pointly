# Cloudflare Pages config for the Pointly marketing site (trypointly.com).
# The static site lives in ./website. Deploy with:  npm run deploy
name = "pointly"
compatibility_date = "2026-07-03"
pages_build_output_dir = "website"
